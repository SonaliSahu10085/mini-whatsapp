<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Mini Whatsapp - All Chats</title>
    <link rel="stylesheet" href="../css/style.css" />
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Dancing+Script:wght@700&display=swap"
      rel="stylesheet"
    />
    <link
      rel="stylesheet"
      href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.7.2/css/all.min.css"
      integrity="sha512-Evv84Mr4kqVGRNSgIGL/F/aIDqQb7xQ2vcrdIwxfjThSH8CSR7PBEakCr51Ck+w+/U6swU2Im1vVX0SVk9ABhg=="
      crossorigin="anonymous"
      referrerpolicy="no-referrer"
    />
  </head>
  <body>
    <div class="nav">
      <div id="heading">Mini Whatsapp</div>
      <div>
        <form method="GET" action="/chat/new">
          <button type="submit">Create New Chat</button>
        </form>
      </div>
    </div>
    <div class="container">
      <% for(let chat of chats) { %>
      <div class="card">
        <p>From: @<span><%=chat.from%></span></p>
        <p>To: @<span><%=chat.to%></span></p>
        <div class="msg"><%=chat.msg%></div>
        <div id="time"><%= chat.created_at.toString().split(" ")[4] %></div>
        <div id="date">
          <%= chat.created_at.toString().split(" ").splice(0,4).join(" ") %>
        </div>
        <div class="icons">
          <form method="GET" action="/chat/<%=chat._id%>/edit">
            <button type="submit">
              <i class="fa-solid fa-pencil"></i>
            </button>
          </form>
          <form method="POST" action="/chat/<%=chat._id%>?_method=DELETE">
            <button type="submit">
              <i class="fa-solid fa-trash"></i>
            </button>
          </form>
        </div>
      </div>
      <% } %>
    </div>
  </body>
</html>
